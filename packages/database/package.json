{
    "name": "@repo/database",
    "version": "0.0.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc --build",
        "clean": "rm -rf dist node_modules"
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.45.0",
        "@supabase/ssr": "^0.5.0"
    },
    "devDependencies": {
        "@repo/tsconfig": "workspace:*",
        "typescript": "^5.5.0"
    }
}