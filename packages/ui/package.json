{
    "name": "@repo/ui",
    "version": "0.0.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "lint": "eslint .",
        "build": "tsc --build",
        "clean": "rm -rf dist node_modules"
    },
    "devDependencies": {
        "@repo/tsconfig": "workspace:*",
        "typescript": "^5.5.0"
    },
    "peerDependencies": {
        "react": "^19.0.0",
        "react-dom": "^19.0.0"
    }
}